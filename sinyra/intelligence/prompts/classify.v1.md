# Classifier Prompt — v1

<!-- TODO(P4): port existing Apps Script classification prompt here -->
<!-- Output contract: JSON with fields: is_feature (bool), feature_type (str), confidence (0-100) -->
<!-- Negative rules: investment_funding_news, leadership_change, tier_list, speculation -->
