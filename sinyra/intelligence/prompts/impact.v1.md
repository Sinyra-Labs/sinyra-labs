# Impact Scorer Prompt — v1

<!-- TODO(P4): port existing Apps Script impact scoring prompt here -->
<!-- Output contract: JSON with fields: impact_score (0-100), impact_label (str), rationale (str) -->
