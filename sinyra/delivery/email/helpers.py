"""Template helpers ported from Apps Script (escape, impactColor, etc.)."""

# TODO(P5): port escape(), impactColor(), impactLabel(), impactBg(),
#           translateType(), getDayNameTR() from the original .gs file
